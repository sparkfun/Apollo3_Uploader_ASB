on:
  push:

name: generate executables

jobs:
  linux:
    name: linux
    runs-on: ubuntu-latest
    steps:
      - name: check out code
        uses: actions/checkout@v2.3.1
        with:
          path: asb
          fetch-depth: 0

      - name: build
        run: |
          echo "I am the linux builder"

  macos:
    name: macos
    runs-on: macos-latest
    steps:
      - name: check out code
        uses: actions/checkout@v2.3.1
        with:
          path: asb
          fetch-depth: 0

      - name: build
        run: |
          echo "I am the macosx builder"

  windows:
    name: windows
    runs-on: windows-latest
    steps:
      - name: check out code
        uses: actions/checkout@v2.3.1
        with:
          path: asb
          fetch-depth: 0

      - name: build
        run: |
          echo "I am the windows builder"
